@model WebLabMVC.Models.Shop

@{
    ViewData["Title"] = "Деталі магазину";
}

<h1>Деталі магазину</h1>

<div class="row">
    <div class="col-md-6">
        <dl class="row">
            <dt class="col-sm-4">Назва магазину</dt>
            <dd class="col-sm-8">@Model.Name</dd>

            <dt class="col-sm-4">Адреса</dt>
            <dd class="col-sm-8">
                @if (!string.IsNullOrEmpty(Model.Address))
                {
                    @Model.Address
                }
                else
                {
                    <span>Не вказано</span>
                }
            </dd>

            <dt class="col-sm-4">Книги у наявності</dt>
            <dd class="col-sm-8">
                @if (Model.Books != null && Model.Books.Any())
                {
                    foreach (var book in Model.Books)
                    {
                        <div>
                            <a asp-controller="Books" asp-action="Details" asp-route-id="@book.Id">
                                @book.Title
                            </a>
                        </div>
                    }
                }
                else
                {
                    <span>Не вказано</span>
                }
            </dd>
        </dl>

        <div class="form-group mt-3">
            <a asp-controller="Shops" asp-action="Index" class="btn btn-secondary">Повернутися до списку магазинів</a>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Редагувати</a>
            <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-sm btn-danger">Видалити</a>
        </div>
    </div>
</div>